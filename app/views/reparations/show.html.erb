<p style="color: green"><%= notice %></p>
      <h1>Voir</h1>
<div class="mb-3">
  <div class="card">
    <div class="card-header">
      <%= render @reparation %>
    </div>
    <div class="card-body">
      <div class="d-flex justify-content-around">
        <%= link_to "Retour", reparations_path, class:"btn btn-outline-primary" %> 
        <%= link_to "Modifier", edit_reparation_path(@reparation), class:"btn btn-warning" %> 
        <%= button_to "Supprimer", @reparation, method: :delete, class:"btn btn-danger" %> 
      </div>
    </div>
  </div>
</div>
